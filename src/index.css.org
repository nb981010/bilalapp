@tailwind base;
@tailwind components;
@tailwind utilities;

/* Global fallback styles (use raw CSS to avoid @apply compatibility issues) */
html, body, #root { height: 100%; }
body { background-color: #0f172a; color: #ffffff; font-family: Inter, sans-serif; }
